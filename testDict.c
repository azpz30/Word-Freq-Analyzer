// Program to test the Dictionary ADT

#include <assert.h>
#include <stdio.h>
#include <stdlib.h>

#include "Dict.h"
#include "WFreq.h"

int main(void) {
	/*Dict d = DictNew();
	DictInsert(d, "G");
	DictInsert(d, "I");
	DictInsert(d, "A");
	DictInsert(d, "B");
	DictInsert(d, "Z");
	DictInsert(d, "Z");
	DictInsert(d, "Z");
	DictShow(d);
	//assert(DictFind(d, "hello") == 1);
	//printf("%s", d->root->words);
	
	DictFree(d);*/
}

